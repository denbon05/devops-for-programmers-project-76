---
- hosts: all

  tasks:
    - name: run redmine
      community.docker.docker_container:
        name: redmine
        image: 'redmine:{{ version | default("5.0.2-alpine") }}'
        restart_policy: always
        state: started
        ports:
          - 80:3000
        env_file: /.env
